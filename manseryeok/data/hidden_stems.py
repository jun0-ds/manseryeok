"""지장간(支藏干) — 각 지지가 품은 천간 + 비중.

단순 비중 가중(월률분야 일수 아님): 본기 0.6 / 중기 0.3 / 여기 0.1.
왕지(子卯酉)는 같은 오행 2지장간이라 0.3 / 0.7. 각 지지 비중 합 = 1.0.

전통 인원용사(人元用事) 표준을 단순 근사. 유파차 있음 — 정밀 일수 배분은 별도(월률분야).
순서: [여기, (중기), 본기] — 본기가 마지막(대표).
"""

from __future__ import annotations

# 지지 char → [(지장간 천간 char, 비중)] (합 1.0)
HIDDEN_STEMS: dict[str, list[tuple[str, float]]] = {
    "子": [("壬", 0.3), ("癸", 0.7)],              # 왕지 — 壬癸 모두 수
    "丑": [("癸", 0.1), ("辛", 0.3), ("己", 0.6)],  # 토(본) + 금 + 수
    "寅": [("戊", 0.1), ("丙", 0.3), ("甲", 0.6)],  # 목(본) + 화 + 토
    "卯": [("甲", 0.3), ("乙", 0.7)],              # 왕지 — 甲乙 모두 목
    "辰": [("乙", 0.1), ("癸", 0.3), ("戊", 0.6)],  # 토(본) + 목 + 수
    "巳": [("戊", 0.1), ("庚", 0.3), ("丙", 0.6)],  # 화(본) + 금 + 토
    "午": [("丙", 0.1), ("己", 0.3), ("丁", 0.6)],  # 화(본) + 토
    "未": [("乙", 0.1), ("丁", 0.3), ("己", 0.6)],  # 토(본) + 화 + 목
    "申": [("戊", 0.1), ("壬", 0.3), ("庚", 0.6)],  # 금(본) + 수 + 토
    "酉": [("庚", 0.3), ("辛", 0.7)],              # 왕지 — 庚辛 모두 금
    "戌": [("丁", 0.1), ("辛", 0.3), ("戊", 0.6)],  # 토(본) + 금 + 화
    "亥": [("戊", 0.1), ("甲", 0.3), ("壬", 0.6)],  # 수(본) + 목 + 토
}
